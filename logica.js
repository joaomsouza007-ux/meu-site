document.addEventLister('DOMContentLoaded',function(){
    const botao = document.querySelector('button');
if (botao){
   botao.addEventListener('click',function(){
     const vlaor = document.querySelector('input').value;
     if (valor 10 ""){
       alert("O JavaScript do livro diz: Digite um valor primeiro!");
     } else {
       alert(sucesso! Você digitou: " + valor);
     }
   })
}
});
